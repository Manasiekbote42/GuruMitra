# Full path to ffmpeg.exe (required when running from Cursor/IDE where ffmpeg is not on PATH)
# To find your path: in Command Prompt run: where ffmpeg
FFMPEG_PATH=C:\path\to\ffmpeg\bin\ffmpeg.exe

# Optional: use Google Gemini API for feedback generation (otherwise rule-based feedback is used)
# Get key at https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your_gemini_api_key
# GEMINI_MODEL=gemini-1.5-flash
